fastapi==0.116.1
uvicorn[standard]==0.35.0   # includes click, httptools, etc.

# PyTorch CPU-only compatible versions (optional, uncomment if you need torch)
# torch==2.8.0+cpu
# torchvision==0.15.2+cpu
# torchaudio==2.8.0+cpu
# --index-url https://download.pytorch.org/whl/cpu

pillow==11.3.0
numpy==2.2.6

chromadb==1.0.20
huggingface-hub==0.34.3
requests==2.32.4
python-dotenv==1.1.1
python-multipart==0.0.20
click  # explicitly added just in case
